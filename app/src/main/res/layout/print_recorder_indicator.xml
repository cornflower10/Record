<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/id_tab_bottom_home"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:descendantFocusability="beforeDescendants"
    android:layout_gravity="bottom"
    android:gravity="center"
    android:clickable="true"
    android:orientation="vertical"
    android:paddingTop="5dp"
    android:paddingBottom="5dp">
    <ImageButton
        android:layout_marginTop="4dp"
        android:id="@+id/btn_tab_bottom_home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:background="@null"
        android:adjustViewBounds="true"
        android:src="@drawable/print_recorder_bottom_drawable"/>
    <TextView
        android:id="@+id/tv_tab_bottom_home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@drawable/home_bottom_textcolor"
        android:textSize="@dimen/text_12_sp"
        android:text="@string/printRecord" />
</LinearLayout>